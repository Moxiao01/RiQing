# 日清（RiQing）Android 端日历应用 —— MVP 需求文档（修订版）

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0-rev（基于 DeepSeek 初稿修订） |
| 适用范围 | MVP（最小可行版本） |
| 平台 | Android（国内应用市场为主，兼顾 Google Play 可选） |
| 状态 | 待评审 |

---

## 〇、修订说明

本文档在 DeepSeek 初稿（以下简称「原稿」）基础上重写。主要补齐：目标用户与成功指标、日历语义（重复/时区/搜索/撤销）、数据模型、Agent 工具契约与确认策略、国内厂商通知/后台适配、隐私边界、验收标准。原稿的版本策略、技术栈选型、MVP 三件套（日历 + 课表 + App 内 Agent）方向予以保留。

文末「与原稿的差异对照」说明改了什么、为什么改，以及本方案自身的问题。

---

## 一、项目概述

**项目名称**：日清（RiQing）

**项目类型**：Android 端本地优先的日历 / 课表 / 待办应用，内置可对话 AI Agent

**核心理念**：以日历为时间轴中心，整合课程管理、待办事项与智能语音交互，做到「每日事、每日清」。

**一句话定位**：给学生用的、能听懂中文口语指令的本地日程助手——课表、作业、会议都在一条时间轴上，用说的就能增删改查。

**版本策略**：

| 版本 | 目标 | 一句话 |
|------|------|--------|
| **MVP** | 跑通核心闭环 | 日/周/月日历 + 课程表 + App 内 Agent + 本地提醒 |
| **V1.1** | 体验补强 | 桌面小组件、课表导入、重复规则增强、Agent 流式与撤销体验 |
| **V2** | 智能化与扩展 | 多端同步、统计、系统级集成、协作 |

---

## 二、目标用户与核心场景

### 2.1 主用户画像（MVP 锁定）

| 画像 | 描述 | 主要诉求 |
|------|------|----------|
| **主用户：大学生** | 有固定课表、作业 DDL、社团/考试节点 | 周课表一眼看清；口语快速记事；到期准时提醒 |
| 次用户：高中生 / 研究生 | 课表结构类似，可能更强调考试倒计时 | 同上，但可后置 |
| 边缘：上班族 | 会议为主，课表弱相关 | 通用日程；V2 再考虑强化 |

**MVP 明确不做**：家庭共享、团队协作、企业日程、多账号。

### 2.2 核心用户故事（Happy Path）

1. **配置学期**：作为学生，我在开学时录入学期起止日期和课表，之后每周课表自动出现。
2. **看今天**：我打开 App 就看到「今天」的日程、课程和未完成待办，不用切换页面。
3. **口头记事**：我对 Agent 说「明天下午三点图书馆还书」，确认后日程就出现在明天 15:00。
4. **查安排**：我问「周五有什么安排」，Agent 列出周五的课 + 日程 + 待办。
5. **改/删**：我说「把还书改到后天同一时间」或「取消今天的社团活动」，确认后生效，错了还能撤销。
6. **准时提醒**：上课前 10 分钟、作业截止前 30 分钟收到本地通知，点开直达详情。
7. **误操作恢复**：Agent 删错了一条日程，我在 10 秒内从通知或结果卡片一键恢复。

### 2.3 成功指标（MVP 验收用）

| 指标 | 定义 | 目标 |
|------|------|------|
| Agent 闭环成功率 | 从自然语言指令到最终数据变更成功的比例（含一次确认） | ≥ 85%（内部测试集 50 条口语指令） |
| 提醒到达率 | 本地通知在目标时间 ±1 分钟内展示的比例（主流厂商机型） | ≥ 95%（完成厂商引导后） |
| 核心路径完成率 | 新用户 10 分钟内完成「配 API → 加一门课 → Agent 加一条待办」 | 内测 8/10 人通过 |
| 崩溃率 | 无崩溃会话占比 | ≥ 99.5% |

---

## 三、MVP 功能范围

### 3.1 基础日历

#### 3.1.1 视图

| 视图 | 要求 |
|------|------|
| 日视图 | 时间轴从 06:00–24:00（可配置起止），展示日程/课程块，底部待办列表 |
| 周视图 | 七列；支持「本周 / 上下周」左右滑动；显示教学周号（若已配置学期） |
| 月视图 | 日期格子内显示圆点/短标题密度；点日期跳日视图 |
| 今日快捷入口 | 底栏或顶栏一键回到「今天」 |

#### 3.1.2 日程（Event）字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 系统生成 | |
| title | String | 是 | ≤ 80 字 |
| startAt | DateTime | 是 | 本地时区语义，见 §3.1.5 |
| endAt | DateTime | 是 | endAt > startAt |
| isAllDay | Boolean | 是 | 默认 false |
| notes | String? | 否 | ≤ 2000 字 |
| reminderOffsets | List\<Int\> | 否 | 相对开始的分钟数；支持 0（准时）、5、15、30、60、1440（1 天前） |
| repeatRule | RepeatRule? | 否 | 见 §3.1.3 |
| createdAt / updatedAt | DateTime | 是 | |
| source | Enum | 是 | MANUAL / AGENT / IMPORT（MVP 仅 MANUAL、AGENT） |
| isDeleted | Boolean | 是 | 软删除，支持撤销 |

**不在 MVP 字段内**：地点地图、参与人、附件、颜色分类（可给一个默认色）。

#### 3.1.3 重复规则（MVP 最小集）

原稿未定义重复规则，导致「每周三高数」无法表达。MVP 支持：

```
FREQ = DAILY | WEEKLY | WORKDAYS
INTERVAL = 1（默认，暂不开放任意间隔）
UNTIL 或 COUNT 至少其一（防止无限重复误创建）
```

- `WORKDAYS` = 周一至周五（节假日调休不在 MVP）
- 单双周放到课表模型（§3.2），不塞进通用 RepeatRule
- 修改重复实例：MVP 仅支持「修改本次」或「删除本次」；「修改整个系列」V1.1

#### 3.1.4 操作与反馈

| 操作 | 行为 |
|------|------|
| 添加 | 日视图空白处点按 / 顶栏 + / Agent |
| 编辑 | 点击事件进入编辑页 |
| 删除 | 确认后软删除；SnackBar 提供「撤销」（10 秒） |
| 搜索 | 按标题关键词 + 日期范围；结果可跳转详情 |
| 冲突提示 | 新建/编辑导致同一时段重叠时提示「与 X 重叠」，**不阻断**保存 |

#### 3.1.5 时区与「今天」

- 全部时间按**设备当前时区**解释与展示
- 用户改时区后：已存事件按**绝对时间戳**不变，展示时换算
- 「今天」= 设备本地日历日
- 跨时区旅行场景不在 MVP 考虑

#### 3.1.6 本地通知

- 渠道（NotificationChannel）必须显式创建：

| channelId | 名称 | 用途 | 重要性 |
|-----------|------|------|--------|
| event_reminder | 日程提醒 | 课程/日程开始前 | HIGH |
| todo_due | 待办截止 | 截止前提醒 | DEFAULT |
| agent_result | Agent 结果 | 长任务完成通知（可选） | LOW |

- Android 12+：申请 `SCHEDULE_EXACT_ALARM`；被拒则降级为不精确闹钟并提示用户去设置
- 首次进入提醒相关功能时，请求通知权限（Android 13+ `POST_NOTIFICATIONS`）
- **国内厂商引导页（必做）**：检测小米/华为/OPPO/vivo/荣耀等，图文引导「允许自启动 / 关闭电池优化 / 锁定后台」，否则提醒会大面积失败

---

### 3.2 课程表（基础版）

#### 3.2.1 概念模型

- **学期（Semester）**：名称、开始日期、结束日期、教学周 1 的周一日期
- **节次时间段（PeriodSlot）**：第 1–N 节的开始/结束时间（全校或用户自定义一套）
- **课程（Course）**：挂在一个学期下，按周次区间生效

> 教学周定义：`weekIndex = (date - week1Monday) / 7 + 1`。周课表与日历都用此定义，避免「日历周」与「教学周」错位。

#### 3.2.2 课程字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 系统生成 | |
| semesterId | FK | 是 | |
| name | String | 是 | |
| teacher | String? | 否 | |
| location | String? | 否 | |
| weekday | Int 1–7 | 是 | 1=周一 |
| periodStart | Int | 是 | 第几节开始 |
| periodEnd | Int | 是 | ≥ periodStart |
| weeks | WeekSet | 是 | 如 [1-16] 或 [1,3,5-8]；MVP 支持连续区间 + 离散列表 |
| isBiweekly | Boolean | 是 | 默认 false；true 时按单/双周过滤 |
| isBiweeklyOdd | Boolean? | 单双周时 | true=单周上，false=双周上 |
| reminderOffsets | List\<Int\> | 否 | 默认 [10]（课前 10 分钟） |
| color | Color token | 否 | 预设 8 色之一 |
| isDeleted | Boolean | 是 | |

#### 3.2.3 与日历的关系（关键设计决策）

**推荐：分表存储，查询时投影。**

- 课程独立表；日历视图在渲染某天时：`events(today) + courses(projected to today)`
- 用户**不能**在日历里直接改投影出来的课程实例（点击进入课程详情/该周实例）
- 允许用户为某节课创建「额外日程」（如调课补课）——用普通 Event 表达，不污染 Course
- 好处：改「第 5 周周三 3-4 节地点」不会误改整个系列；Agent 对「课」和「事」的工具也可分开

#### 3.2.4 周课表视图

- 独立 Tab「课表」：7 列 × N 节网格
- 顶部显示「第 X 教学周」，可左右切换周次
- 未配置学期时显示空态引导

#### 3.2.5 MVP 不做的课表能力

Excel/教务导入、自动解析截图、调课、代课、教室占用、成绩关联 → 全部 V1.1+。

---

### 3.3 待办（Todo）

与日程区分：待办强调**是否完成**与**截止**，不一定占用时间块。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | |
| title | String | 是 | |
| dueAt | DateTime? | 否 | 无截止则归入「今天/收件箱」 |
| notes | String? | 否 | |
| isDone | Boolean | 是 | 默认 false |
| doneAt | DateTime? | 完成时 | |
| reminderOffsets | List\<Int\> | 否 | 相对 dueAt |
| createdAt / updatedAt | | 是 | |
| source | | 是 | MANUAL / AGENT |
| isDeleted | Boolean | 是 | |

MVP 不做：优先级 P0-P3、标签、子任务、重复待办（可用重复日程代替）。

---

### 3.4 内置 AI Agent（核心闭环）

#### 3.4.1 定位

- App 内对话界面，**非**系统语音助手
- 通过大模型 API（Function Calling / Tools）把中文指令映射为本地 CRUD
- **本地数据是唯一事实源**；Agent 只是操作入口之一

#### 3.4.2 能力边界（MVP）

| 能做 | 做不到（明确拒答或降级） |
|------|--------------------------|
| 增删改查日程、待办、课程 | 联网搜索、写邮件、打开其他 App |
| 按日期/关键词列出安排 | 跨设备操作、云同步 |
| 基于已录入内容的简单汇总（如「今天有几节课」） | 未配置 API 时的任何「智能」回答 |

#### 3.4.3 工具清单（Function Calling）

模型侧只暴露下列工具；参数必须过本地校验后才能写库。

| 工具名 | 参数（摘要） | 风险级 |
|--------|--------------|--------|
| `list_events` | start, end, keyword? | 低 |
| `create_event` | title, start, end?, notes?, reminders?, repeat? | 低 |
| `update_event` | event_id, fields… | 中 |
| `delete_event` | event_id | 高 |
| `list_todos` | date?, include_done? | 低 |
| `create_todo` | title, due?, notes?, reminders? | 低 |
| `update_todo` | todo_id, fields… | 中 |
| `complete_todo` | todo_id | 低 |
| `delete_todo` | todo_id | 高 |
| `list_courses` | week? or date? | 低 |
| `create_course` | semester_id?, name, weekday, periods, weeks, … | 中 |
| `update_course` | course_id, fields… | 中 |
| `delete_course` | course_id | 高 |
| `get_semester` | — | 低 |

**时间参数一律要求模型输出 ISO-8601 本地时间**（如 `2026-08-12T15:00:00`）。若用户只给了模糊说法且模型无法解析，必须走反问，禁止猜。

#### 3.4.4 确认与撤销策略

| 风险级 | 策略 |
|--------|------|
| 低（创建、查询、标记完成） | 直接执行；结果卡片可「撤销」（10 秒） |
| 中（单条修改） | 执行前展示字段级 diff，用户点「确认修改」 |
| 高（单条删除） | 展示将删除的标题/时间，确认后软删除，可撤销 |
| 高（批量删除，如「删掉今天所有待办」） | **强制**列出全部条目 + 二次确认 |

原则：**能撤销就少确认**；批量与删除必须确认。撤销 = 写回软删除/修改前快照，不是「再让模型改回去」。

#### 3.4.5 对话 UX 要点

- 解析失败：Agent 反问（「你是要明天 15:00 还是今天 15:00？」），不静默失败
- 确认卡片必须展示**绝对本地时间**（2026-08-12 15:00），不能只回显「明天下午」
- 操作成功：结果卡片含「查看 / 撤销 / 继续」
- 会话内上下文：MVP 支持同一会话指代（「再加一个一样的，改到周五」）；跨会话记忆不做
- 流式输出：V1.1；MVP 可一次性返回

#### 3.4.6 失败与降级

| 场景 | 行为 |
|------|------|
| 未配置 API | 空态引导去「设置 → AI」；提供「连接测试」 |
| 超时 / 5xx | 文案 + 重试按钮；不写入半成品数据 |
| 工具参数校验失败 | 返回错误给模型一轮修复；仍失败则终止并提示用户改说法 |
| 断网 | 聊天可输入，发送时提示离线；本地 CRUD 不受影响 |

---

## 四、AI 服务配置与隐私

### 4.1 配置项（设置页）

| 配置项 | 说明 | 默认 |
|--------|------|------|
| Endpoint URL | OpenAI 兼容优先；非兼容走适配器 | 空 |
| API Key | 加密存储 | 空 |
| Model 名称 | 字符串 | 空 |
| 超时（秒） | 10–120 | 30 |
| Temperature（可选） | 0–2 | 0.2（偏事实操作） |

- 存储：DataStore + Android Keystore 加密；**禁止**写入日志、崩溃上报、备份明文
- 「连接测试」：发一条最小 chat/completions 或 models 请求，展示 HTTP 状态与延迟
- 供应商：MVP 以 **OpenAI 兼容协议** 为第一公民（含通义兼容模式、DeepSeek、Moonshot 等）；文心等原生协议放适配层，可后置

### 4.2 隐私边界（必须写进产品文案）

1. **本地优先**：日程/待办/课程默认只存本机 Room。
2. **出网内容**：仅在用户主动与 Agent 对话时，将**当前对话必要文本 + 工具结果上下文**发送至用户自配置的 API。
3. **明示**：首次使用 Agent 时弹出说明：「将把你的对话内容发送到你配置的模型服务」。
4. **可关**：设置中可一键停用 Agent，本地日历功能不受影响。
5. **无隐式上传**：MVP 无埋点上报事件内容；若做匿名使用统计，仅本地或单独征得同意。
6. **备份**：设置中支持导出 JSON（手动）；不自动云备份。

---

## 五、平台架构与技术选型

### 5.1 SDK 版本

| 配置项 | 值 | 说明 |
|--------|-----|------|
| compileSdk | 36（Android 16） | 与 target 一致 |
| targetSdk | 36（Android 16） | 满足商店对新提交的要求 |
| minSdk | 26（Android 8.0） | 覆盖约 95%+；通知渠道与后台限制 API 可用 |

> 覆盖率数据请以提交前的官方 Dashboard / 后台为准；上表分布仅作选型参考，不写入对外文案。

如必须覆盖更老设备，可评估 minSdk 24，但需验证 AGP 版本支持与通知兼容成本。**不建议**为覆盖率降到 23。

### 5.2 技术栈

| 层 | 选型 |
|----|------|
| 语言 | Kotlin |
| UI | Jetpack Compose + Material 3 |
| 架构 | 单 Activity + MVVM + Repository |
| DI | Hilt |
| 异步 | Coroutines + Flow |
| 本地库 | Room（事件/待办/课程/学期） |
| 偏好/配置 | DataStore（加密 API Key） |
| 网络 | OkHttp + Retrofit（或 Ktor Client，二选一，团队定） |
| 序列化 | kotlinx.serialization |
| 导航 | Navigation Compose |
| 最低通知 | NotificationManager + WorkManager（可选延迟任务） |

### 5.3 建议模块划分

```
:app
:core:model          // Event / Todo / Course / Semester
:core:database       // Room
:core:datastore      // 设置与密钥
:core:designsystem   // 组件与主题
:feature:calendar    // 日/周/月
:feature:timetable   // 课表
:feature:agent       // 聊天 + 工具执行器
:feature:settings
:core:ai             // API 客户端、Provider 适配、Tool schema
```

### 5.4 工程约束

- 所有写库操作在 Repository 层统一 `softDelete` / `restore`
- Agent 工具执行必须在本地 `ToolValidator` 校验（时间范围、标题长度、外键存在）后再调用 DAO
- 禁止把 API Key 打进 Log / Timber / 崩溃平台
- 关键路径单元测试：重复规则展开、教学周计算、时间解析映射、Tool 校验

---

## 六、权限清单

| 权限 | 时机 | 拒绝后果 |
|------|------|----------|
| `POST_NOTIFICATIONS` | 首次设置提醒或进入 Agent 结果通知前 | 无提醒；功能仍可用 |
| `SCHEDULE_EXACT_ALARM` | Android 12+ 设置精确提醒时 | 降级不精确提醒 + 引导去系统设置 |
| 厂商自启动/电池白名单 | 引导页手动开启（非系统 runtime permission） | 后台可能杀进程导致漏提醒 |
| 网络 | 安装即有 | Agent 不可用，本地功能可用 |

MVP **不申请**：通讯录、存储、日历读写（系统日历同步是 V2）、麦克风（系统语音是后续版本）。

---

## 七、信息架构（页面级）

```
Bottom Navigation
├── 今日（Home）
│   ├── 日期头 + 农历/节气占位（V1.1+）
│   ├── 今日时间轴（课程 + 日程）
│   └── 今日待办
├── 日历（Calendar）
│   ├── 日 / 周 / 月 切换
│   ├── 搜索
│   └── 点空白 / 点事件 → 编辑
├── 课表（Timetable）
│   ├── 周网格 + 教学周切换
│   └── 课程详情 / 本学期课程列表
├── Agent
│   ├── 对话流
│   ├── 确认卡片 / 结果卡片
│   └── 首次授权与空态引导
└── 我的 / 设置
    ├── AI 配置（Endpoint / Key / Model / 超时 / 测试）
    ├── 提醒与厂商适配引导
    ├── 学期与节次管理
    ├── 备份导出
    └── 关于与隐私说明
```

---

## 八、MVP 验收清单（DoD）

- [ ] 日/周/月切换流畅，今日数据正确
- [ ] 日程 CRUD + 软删除撤销
- [ ] 至少支持每日/每周/工作日重复
- [ ] 待办 CRUD + 完成状态 + 截止提醒
- [ ] 学期配置后，课程按教学周正确投影到日历与课表
- [ ] 课程 CRUD（含周数、节次、单双周）
- [ ] 本地通知在目标时间弹出（厂商引导完成后）
- [ ] 设置中可配置 API 并连接测试成功
- [ ] Agent 能完成：创建日程、改时间、删日程、列今日、加待办、完成待办、加课
- [ ] 修改/删除有确认或可撤销；批量删除二次确认
- [ ] 未配置 Key、断网、超时有明确 UI
- [ ] API Key 不出现在日志与明文备份
- [ ] 无账号也能完整使用

---

## 九、下一版本（V1.1 / V2）

### 9.1 V1.1（体验补强，按优先级）

1. **桌面小组件**：今日待办 / 今日课表 / 月历
2. **课表导入**：Excel / CSV 模板；教务系统适配另议
3. **重复规则增强**：自定义间隔、按月、修改整个系列
4. **Agent 体验**：流式回复、更丰富的确认动画、错误自修复
5. **备份恢复**：JSON 导入
6. **农历 / 节假日调休显示**（仅展示，不改排课逻辑）

### 9.2 V2（智能化与扩展）

- 多端同步（账号 + 端到端加密）
- 与系统日历（Calendar Provider）双向同步
- 数据统计：完成率、时间分配
- 主动智能：每日摘要、冲突提示、历史行为推荐时间
- 系统级入口：快捷方式、App Shortcuts、（海外）App Actions
- 协作共享日历
- 离线语音唤醒 + 本地指令解析（强依赖端侧模型，单独立项）
- 习惯打卡、番茄钟等独立模块（观察池，不承诺进 V2）

### 9.3 观察池（有需求再立项）

考试倒计时专页、作业附件、情绪化指令、教室地图、多课表并存、iOS/Web。

---

## 十、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 国内厂商杀后台 | 漏提醒，用户流失 | 首启引导 + 定期检测 + 前台服务仅在必要时（谨慎） |
| 中文时间解析不准 | Agent 口碑差 | 白名单表达式 + 必须反问 + 确认卡片绝对时间 |
| API 成本与稳定性 | 体验抖动 | 超时/重试、本地 CRUD 不依赖云、可换供应商 |
| 课表与日程数据混乱 | 改课误伤日程 | 分表 + 投影；工具分开 |
| 误删 | 信任崩塌 | 软删除 + 撤销 + 批量二次确认 |
| 隐私质疑 | 合规/口碑 | 首次明示、可关闭 Agent、Key 加密 |

---

## 十一、后续产出物（建议顺序）

1. 字段字典与 Room ER 图（可从本文 §3 直接展开）
2. 核心页面线框（今日 / 周历 / 课表 / Agent 确认卡片）
3. Agent System Prompt + Tool JSON Schema
4. 时间解析测试用例集（≥ 50 条中文口语）
5. 厂商通知适配检查表（机型 × 设置项）

---

# 附录 A：与 DeepSeek 原稿的差异对照

| 主题 | DeepSeek 原稿 | 本修订版 | 为何改 |
|------|---------------|----------|--------|
| 用户与场景 | 笼统「个人时间管理」 | 锁定大学生；7 条用户故事 | 决定课表优先级与文案 |
| 成功指标 | 无 | 4 个可验收指标 | MVP 是否「完成」需要尺子 |
| 重复规则 | 未提 | 每日/每周/工作日 + UNTIL/COUNT | 否则课表型日程无法表达 |
| 课表与日历关系 | 「课程在日历可见」未说存储 | **分表 + 投影**；禁止直接改投影实例 | 避免改课污染手建日程 |
| 教学周 | 「按学期/周次」未定义 | 明确 week1Monday 计算公式 | 周课表与日历对齐的前置条件 |
| 单双周 | 放在 V1.1 | MVP 就支持 isBiweekly | 国内课表刚需；放 V1.1 会导致 MVP 课表残缺 |
| 待办模型 | 混在「日程/待办」口头列表 | 独立 Todo 字段表 | 完成态与截止提醒语义不同 |
| 搜索 / 冲突 / 撤销 | 未写 | 搜索必做；冲突只提示；软删除+撤销 | 「查」与防误操作是 Agent 闭环刚需 |
| Agent 确认 | 「操作前确认」一刀切 | 按风险分档；**创建少确认、删除可撤销、批量必确认** | 一刀切会让 Agent 又慢又烦 |
| Agent 工具 | 仅概念 | 14 个工具 + 风险级 + ISO 时间约束 | 没有契约无法开发与测试 |
| 时间解析 | 未提 | 失败必须反问；卡片显示绝对时间 | 中文口语 NLU 最大失败点 |
| 会话记忆 | 全部放 V2 | MVP 同会话指代，跨会话不做 | 无指代则像关键词机器人 |
| API 配置 | Endpoint/Key/Model/超时 | 保留 + 连接测试 + OpenAI 兼容优先 + Temperature | 国内多数兼容 OpenAI 协议，降低适配成本 |
| 隐私 | 「本地优先、不上传」与大模型调用矛盾未解 | 明确出网内容、首次明示、可关闭 Agent | 合规与信任问题必须写清 |
| 通知渠道 | 未写 | 3 个 channel + 精确闹钟 | 实现必做项 |
| 国内厂商后台 | 未写 | 引导页必做 | 不写等于提醒功能名存实亡 |
| 备份 | 未写 | MVP 手动导出 JSON | 纯本地数据丢失风险 |
| 系统语音 | V1.1 主打 | 降级为 V2；V1.1 改为小组件/导入 | 国内无 Google Assistant，App Actions 路径窄 |
| citation | 有 `[citation:n]` 无列表 | 全部删除，SDK 结论自洽表述 | 假引用无法过评审 |
| 权限 | 未列 | 专章 + 不申请清单 | 避免过度权限 |
| 验收 | 无 | DoD 勾选清单 | 测试与结项依据 |
| 模块与测试 | 仅栈名 | 模块划分 + 必测点 | 可直接开工 |

---

# 附录 B：本方案的好处与代价

## B.1 相对原稿的好处

1. **可开发**：数据模型、工具契约、页面 IA 齐了，Android 与后端/模型侧可以并行拆任务。
2. **可验收**：成功指标 + DoD，避免「好像做完了」。
3. **风险前置**：把「国内通知失效」「中文时间解析」「误删」写成产品机制，而不是留给开发现场拍脑袋。
4. **体验更像产品**：确认分档 + 撤销，比「每步都点确认」更接近成熟日程 App。
5. **隐私自洽**：承认会出网，并规定出网什么、怎么告知，避免「本地优先」口号被一句 API 调用打脸。
6. **版本更稳**：V1.1 不去赌系统语音，先做小组件与导入这类确定性收益。

## B.2 本方案自身的问题与代价

| 问题 | 说明 | 可能的缓解 |
|------|------|------------|
| **范围仍偏大** | 即便砍了导入与语音，「日历+课表+待办+Agent+通知适配」对小团队仍重 | 再切一版「Internal Alpha」：先日程+Agent，课表只读录入 |
| **分表投影更复杂** | 日/周视图要合并 Event 与 Course，查询与点击路由更难 | 投影逻辑收在单一 `DayAgendaRepository`；写测试锁行为 |
| **重复规则简化** | 不支持任意 INTERVAL、按月、系列修改，高级用户会觉得弱 | 文案标明 MVP 能力；V1.1 补 |
| **单双周进 MVP** | 增加录入与过滤复杂度 | UI 提供「单周/双周/每周」三选一，不做更复杂规则 |
| **同会话指代** | 服务端无记忆时要在端上拼对话历史与工具结果，token 与实现成本上升 | 限制历史轮数；压缩为结构化状态 |
| **厂商适配做不完** | 品牌/机型矩阵大，无法穷尽 | 只保证 Top 品牌文案 + 通用电池优化入口；其余靠用户反馈 |
| **OpenAI 兼容优先** | 文心等原生协议用户首次配置门槛高 | 设置里给「兼容模式」说明与示例 |
| **无账号** | 换机即丢数据 | MVP 靠手动导出；把云同步明确留给 V2 并在关于页说明 |
| **指标可能测不准** | 「提醒到达率」依赖真机值守 | 内测机型清单 + 自动化部分降级为「发出率」 |
| **文档更长** | 评审时间变长 | 用附录对照表做 diff 评审，正文按模块拆开读 |

## B.3 建议的采用方式

- **产品/商务评审**：读 §一–§四、§七、§八 + 附录 A
- **Android 开发**：读 §三、§五–§八
- **模型/Prompt**：读 §3.4 + 附录后续产出物 3、4
- **不必**把附录 B 发给外部用户

---

## 附录 C：待产品负责人拍板的决策（已给出默认）

| # | 决策点 | 默认建议 | 若反对需说明 |
|---|--------|----------|--------------|
| 1 | 主用户 | 大学生 | 影响课表是否第一优先级 |
| 2 | 重复日程是否进 MVP | **是**（每日/每周/工作日） | 否则课表型使用不成立 |
| 3 | 课程与日程存储 | **分表 + 投影** | 合并成 Event 子类型会简化查询但难撤销/难分工具 |
| 4 | 删除策略 | 软删除 + 可撤销；批量二次确认 | 若坚持「先确认再删」需接受更低操作频率 |
| 5 | 分发渠道 | 国内市场为主，Google Play 可选 | 影响统计、语音、GMS 依赖 |
| 6 | 单双周 | **进 MVP** | 拒绝则课表完整性受损，需接受 |
| 7 | 系统语音助手 | **不进 V1.1**，放 V2 | 若强需求则 V1.1 只做快捷方式/App Shortcuts，不做 Assistant |

---

*文档结束。如需下一步，建议优先产出：① ER 图与字段字典 ② Agent Tool JSON Schema ③ 今日/周历/Agent 确认卡片线框。*
